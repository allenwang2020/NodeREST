   web:
     build: .
     ports:
       - 8080:8080
     links:
       - db-mongo 
   db-mongo:  
     image: mongo
     ports:
       - 27017:27017
     volumes:
       - /data/db/mongo/products-demo:/data/db/mongo/products-demo

